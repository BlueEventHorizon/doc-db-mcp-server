# .claude/.doc-advisor/toc/specs-7846086ae9fc/toc.yaml
# Document Search Index for key: specs
# Auto-generated by doc-advisor index-docs - Do not edit directly

metadata:
  name: Document Search Index (specs)
  key: specs
  generated_at: 2026-06-21T05:00:08Z
  file_count: 2

docs:
  docs/specs/base/design/DES-001_doc_db_mcp_server_design.md:
    title: DES-001 doc-db MCP Server Design
    purpose: Specifies the complete architecture, data model, search pipeline, and implementation guidelines for a hybrid search MCP server in Go, providing vector embedding plus BM25 plus LLM reranking capabilities for markdown documents
    content_details:
      - Package architecture with layered dependencies
      - SQLite schema with WAL mode concurrency control
      - Embedding record and series_keys multi-to-many management
      - Hybrid search pipeline with vector, BM25, and LLM reranking
      - Chunker with markdown heading boundary splitting
      - BM25 tokenizer with ID pattern matching and Unicode normalization
      - OpenAI Embedding API integration with batch handling and retry logic
      - Expiry worker with TTL and LRU policies per key
      - SSRF protection for URL fetching
      - Configuration via environment variables and error handling policies
    applicable_tasks:
      - MCP server implementation
      - Go backend development
      - Search system design
      - SQLite database schema
      - Embedding pipeline design
      - Hybrid search implementation
      - API integration
    keywords:
      - MCP
      - doc-db
      - Go
      - SQLite
      - embedding
      - BM25
      - hybrid search
      - reranking
      - Streamable HTTP
  docs/specs/base/requirements/APP-001_doc_db_mcp_server_requirements.md:
    title: APP-001 doc-db MCP Server Requirements
    purpose: Specifies functional and non-functional requirements for a hybrid document search MCP server that processes Markdown, manages embeddings, and provides vector plus lexical plus LLM rerank search capabilities.
    content_details:
      - Hybrid search architecture with vector embedding, lexical matching, and LLM reranking stages
      - OpenAI API key configuration with fallback mechanism (OPENAI_API_DOCDB_KEY / OPENAI_API_KEY)
      - upsert_documents tool with support for direct content or URL-based document ingestion
      - Chunk division at Markdown heading boundaries (H1–H6) with heading hierarchy paths
      - Differential embedding management with hash-based deduplication and series_keys sharing
      - Lexical matching algorithm for ID patterns and entity name exact matches
      - LLM Rerank fallback when embedding plus lexical scores are combined
      - delete_documents tool with series-aware record cleanup and physical deletion
      - query tool with mode selection (emb / lex / hybrid / rerank) and top_n limiting
      - TTL and LRU capacity-based expiration policies with per-KEY override capability
    applicable_tasks:
      - Define document search system architecture
      - Plan embedding and vector storage design
      - Implement MCP tool interface for document operations
      - Design hybrid search scoring algorithm
      - Set up OpenAI API integration for embeddings and reranking
      - Implement chunk-level metadata tracking and series management
      - Specify error handling and recovery scenarios
    keywords:
      - MCP server
      - hybrid search
      - vector embedding
      - lexical matching
      - LLM rerank
      - chunk division
      - series deduplication
      - OpenAI API
      - expiration policy
      - lexical scoring
