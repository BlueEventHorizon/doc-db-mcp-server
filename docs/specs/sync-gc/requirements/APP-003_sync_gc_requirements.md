---
type: temporary-feature-requirement
notes:
  - この文書が正。旧仕様（APP-001・DES-001・実装コード）と矛盾する場合はこの文書を優先して判断・実装すること。
  - 旧仕様ファイルは本 feature 実装完了まで書き換えない。新規ファイル / 新規ディレクトリとして切り出すこと。
  - 本 feature 実装完了後、この文書は APP-001 へ merge され削除される予定。
---

# APP-003 desired-state 同期ジョブ + 削除予約の起動時ガベージコレクション 要件定義書

## 概要

`upsert_documents`（APP-001 FNC-001）は「渡されたドキュメントを追加・更新する」追加専用の操作であり、クライアント側でファイルが削除された場合にそれを検出してインデックスから除去する仕組みを持たない。その結果、削除済みファイルの record が無期限にインデックスへ残留し、検索結果に汚染が生じる問題がある。

また既存の EXP-01/02（TTL/LRU）は `last_accessed_at` という推測的な指標に基づく KEY 単位の自動廃棄であり、削除済み事実を確定的に検出する手段ではない。仕事で使うドキュメント検索において、推測ベースで必要なデータが消えることは許容できない（実際に、bulk 投入で `max_chunks` を超過し、作成直後の KEY が LRU により誤って削除される事故が発生している）。

本 feature は、クライアントが確実に把握している事実（「このファイル一覧が現在の全て」「この series はもう使われない」）だけを根拠に、不要データを除去する仕組みを追加する。**APP-001 TBD-009**（series の廃棄ポリシー未確定）はこの feature で解決する。

既存の `upsert_documents` / `delete_documents` / `delete_series` および EXP-01/02（TTL/LRU タイマーワーカー）は、**処理ロジック本体を変更しない**。ただし SYN-08（KEY 単位排他）の実現のため、これらの呼び出しを KEY 単位排他ロックで包むラップのみ追加する（判定・削除・差分管理のロジック自体は無改造。詳細は設計書 DES-003 §3.5）。

## 要件一覧

### FNC-006 desired-state 同期ジョブ + 削除予約の起動時ガベージコレクション

MCP ツールとして `sync_documents` と `get_sync_status`、`schedule_delete_series` を提供する。

#### 入力パラメータ（sync_documents）

| パラメータ | 型     | 必須 | 説明                                                                                                                                   |
| ---------- | ------ | ---- | -------------------------------------------------------------------------------------------------------------------------------------- |
| key        | string | ✓    | 対象インデックスの KEY                                                                                                                 |
| series     | string | ✓    | 対象の時系列キー                                                                                                                       |
| documents  | array  | ✓    | 当該 key・series の**完全な現在状態**を表すドキュメントのリスト。要素形式は upsert_documents と同一（content / url / local_path 排他） |

#### 処理

| ID     | 要件                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SYN-01 | サーバーは `sync_documents` の `documents` を、当該 key・series の完全な現在状態（desired-state）とみなす                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| SYN-02 | `documents` の各要素には、既存の upsert_documents と同一の差分管理（APP-001 DIF-01〜03）を適用する。無改造で再利用する                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| SYN-03 | 処理完了後、当該 key・series に既存登録されているが `documents` に含まれない path を検出し、当該 series との紐付けを**即座に**除去する（当該 series を指定した検索の結果に削除済みファイルが混入しないようにするため）。紐付け除去の結果どの series からも参照されなくなった record は、その場では物理削除せず削除予約として記録する（物理削除は GC-02 の起動時スイープで行う。record を残すのは SYN-04 の自己修復を Embedding 再計算なしで可能にするため）。なお series を指定しない検索（KEY 全体検索）には、物理削除まで当該 record が含まれ得る（既知の制約）                                                                                                                                                                                                                                                                                                                                                                                                          |
| SYN-04 | 過去に削除予約された path が、その後の `sync_documents` 呼び出しの `documents` に再度含まれ、**かつその path の処理が成功した場合**、削除予約を解除する（誤操作からの自己修復。処理が失敗した path の予約は解除しない）。予約解除の際は、当該 path に物理削除待ちの旧 record（どの series からも参照されないもの）が残っていれば先に回収してから解除し、**解除によって回収漏れが生じないこと**を保証する。record は物理削除（起動時スイープ）まで残っているため、内容が不変（content_hash 一致）であれば SYN-02 の差分管理（DIF-02）が record を再発見して series を再紐付けし、Embedding の再計算（API 課金）は発生しない。内容が変わって復活した場合は通常の内容変更（DIF-03）として処理され、旧 record は既存の同一 path 掃除機構により物理削除される（orphan record は残らない）。当該 key・series に GC-01 由来の **series 全体の削除予約**が存在する場合も、`sync_documents` が呼ばれた時点でその series はまだ使われていると判断し、series 全体の削除予約を解除する |
| SYN-05 | `sync_documents` は処理完了を待たず、一意な job_id を即座に返す。実際の処理はサーバー内部で継続する                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| SYN-06 | `get_sync_status` は job_id を受け取り、ジョブの進捗（処理済み・スキップ・失敗件数、削除予約件数）および完了有無を返す。存在しない job_id・保持期限切れの job_id を指定された場合はエラーを返す                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| SYN-07 | ジョブの状態はメモリ上にのみ保持し、永続化しない。サーバー再起動でジョブ状態が失われても、クライアントが再度 `sync_documents` を呼び出せば冪等に補われるため実害はない                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| SYN-08 | 同一 KEY に対する `sync_documents` は、**同じ KEY を対象とする他の全ての書き込み系操作**と**排他的に**実行する。対象は series を問わず KEY 全体に及ぶ: `upsert_documents` / `delete_documents` / `delete_series` / `schedule_delete_series` / 別の `sync_documents`（同一 KEY 内の別 series も含む）に加え、KEY 全体を削除する `delete_index`（APP-001 MNG-02）・EXP-01（TTL）・EXP-02（LRU）による自動削除も対象に含む。desired-state 判定（ドキュメント処理→既存 path 一覧取得→削除予約）を単一の論理的な処理単位として扱い、途中に KEY の一部または全部を変更する操作が割り込んで desired-state と実データがずれることを防ぐ。排他の粒度は KEY 単位とする（同一 KEY 内の異なる series 間であっても並行実行しない。理由は設計書参照）                                                                                                                                                                                                                                    |

#### 出力仕様

| ツール                   | 出力                                                                                                                                                                                                |
| ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `sync_documents`         | `{job_id: string}`。処理完了を待たない即時応答（SYN-05）                                                                                                                                            |
| `get_sync_status`        | `{status: "running" \| "done" \| "failed", processed: int, skipped: int, failed: int, deleted_paths_marked: int, errors: string[]}`。存在しない・保持期限切れの job_id の場合はエラー応答（SYN-06） |
| `schedule_delete_series` | `{key: string, series: string, already_scheduled: bool}`。即時削除は行わない（GC-01）。`already_scheduled` は呼び出し時点で既に同一 series の削除予約が存在した場合に true（冪等呼び出しの判別用）  |

#### 削除予約と起動時ガベージコレクション

| ID    | 要件                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| GC-01 | `schedule_delete_series` は指定 key・series を即座に削除せず削除予約として記録する MCP ツール。branch 削除等、クライアントが series の廃止を確定的に把握しているケース向け。既存の `delete_series`（即時削除）とは別ツールとして共存する                                                                                                                                                                                                                                                                                      |
| GC-02 | series 単位（GC-01 由来）・path 単位（SYN-03 由来）いずれの削除予約も、サーバー起動時に一括して物理削除する。path 単位の予約は SYN-03 で series との紐付けを除去済みの record（どの series からも参照されない record）の物理削除を意味し、**どの series からも参照されない record のみを削除する**（series の紐付きが残る record・予約後に復活した record には一切触れない。予約が古くなって残っていても実データを壊さない）。series 単位の物理削除は既存の DEL-03 と同じ安全条件（他 series が参照する record は保持）に従う |
| GC-03 | 起動時の削除予約処理は、起動時統計情報（MNG-01 相当の集計値）の算出より前に完了させる（統計値が削除予約処理後の正しい状態を反映するようにするため）                                                                                                                                                                                                                                                                                                                                                                           |
| GC-04 | 削除予約の物理削除に個別失敗した場合、ログに記録し処理を継続する。失敗した予約は次回起動時に再試行される                                                                                                                                                                                                                                                                                                                                                                                                                      |
| GC-05 | `sync_documents` のバックグラウンド処理は、リクエストの生存期間から切り離された独立した context で実行する。MCP リクエストへの応答（job_id 返却）後にクライアントが切断してもジョブは継続する。サーバーがシャットダウンする場合はジョブを中断し、当該ジョブの状態を `"failed"` にする（進行中のまま応答が返らなくなることを避ける）                                                                                                                                                                                           |

### 既存要件への影響（旧仕様との関係。merge 時に反映）

| 対象            | 変更内容                                                                                                                                                                               |
| --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| APP-001 EXP-01  | TTL 削除の実行前に、SYN-08 の KEY 単位排他ロックを取得する旨を追記する                                                                                                                 |
| APP-001 EXP-02  | LRU 削除の実行前に、EXP-01 と同様の排他ロックを取得する旨を追記する                                                                                                                    |
| APP-001 TBD-009 | 「解決: FNC-006（GC-01〜05）で、TTL/LRU の推測ベース拡張ではなく、クライアント確定情報に基づく削除予約＋起動時スイープ方式を採用」に更新する                                           |
| DES-001 §8      | TTL/LRU（EXP-01/02）の判定ポリシー・処理ロジック本体は変更しない旨（削除実行前の KEY 単位ロック取得のみ追加する、SYN-08）と、本 feature の設計書（DES-003 相当）への参照注記を追加する |
| DES-001 §8.3    | 「series 廃棄ポリシー（TBD）」を「解決済み」に更新し、採用しない代替案（TTL/LRU の series 拡張）を明記する                                                                             |
| DES-001 §5.1    | ユースケース一覧に `sync_documents` / `get_sync_status` / `schedule_delete_series` を追加する                                                                                          |

## エラーケース

| 条件                                                            | 動作                                                                    |
| --------------------------------------------------------------- | ----------------------------------------------------------------------- |
| 存在しない・保持期限切れの `job_id` で `get_sync_status` を呼ぶ | エラーを返す（SYN-06）                                                  |
| `sync_documents` 処理中に同一 KEY への他の書き込みが要求される  | SYN-08 により `sync_documents` の完了までブロックされる（拒否ではない） |
| サーバーシャットダウン中に `sync_documents` が実行中            | ジョブを中断し `status: "failed"` にする（GC-05）                       |

## TBD

| ID      | 内容                                                                                                  | 解決 |
| ------- | ----------------------------------------------------------------------------------------------------- | ---- |
| TBD-101 | 完了済みジョブ（`SyncJobStatus`）の保持ポリシー（件数上限 or 経過時間）の具体的な値は設計書で確定する | -    |

## 変更履歴

| 日付       | 変更者  | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ---------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-03 | k2moons | 初版作成（APP-001 に直接追記していた FNC-006 を、additive_development_spec.md §1 の判定基準（分離管理する価値: 旧仕様との一時的な矛盾・実装完了後の統合が必要な規模の変更）に基づき本 feature へ retrofit）                                                                                                                                                                                                                                                                                     |
| 2026-07-03 | k2moons | レビュー対応: SYN-08（KEY 単位排他）・SYN-04 自己修復・出力仕様・GC-05（context 分離）を追加（旧 APP-001 編集履歴を統合）                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-07-04 | k2moons | レビュー対応(追補): SYN-08 の排他対象漏れ（delete_index・TTL・LRU）を修正しロック粒度を KEY 単位に明確化。`schedule_delete_series` 出力を具体化（旧 APP-001 編集履歴を統合）                                                                                                                                                                                                                                                                                                                    |
| 2026-07-04 | k2moons | レビュー対応(検索最新性): SYN-03 を「削除予約のみ（次回起動まで検索結果に残る）」から「series 紐付けの即時除去 + orphan record の物理削除予約」に変更。sync 完了直後の series 指定検索に削除済み path が混入する問題を解消。SYN-04 に自己修復が Embedding 再計算なしで成立する根拠（record 残存 + DIF-02）を明記。GC-02 に path 単位予約の意味（切り離し済み record の物理削除）を反映。GC-01（series 単位）は誤操作時の影響範囲が branch 全体に及ぶため遅延方式を維持                          |
| 2026-07-04 | k2moons | レビュー対応(orphan リーク疑義): 指摘（別内容復活時のリーク・record 実体単位予約の必要性）を実コード検証の結果不成立と判断（既存の同一 path 掃除機構が旧 orphan を物理削除する。DES-003 1.7 参照）。検証で見つかった実在の残穴に対し SYN-04 に「予約解除は処理成功 path のみ」を追加。別内容復活時の旧 record の扱い（DIF-03 + 掃除機構で物理削除）を SYN-04 に明記。概要の「既存ツールは変更しない」を「処理ロジック本体を変更しない（排他ロックのラップのみ追加）」に限定し設計との矛盾を解消 |
| 2026-07-04 | k2moons | レビュー対応(CleanOtherSeries 失敗時の残穴): SYN-04 に「予約解除の際は物理削除待ちの旧 record を先に回収し、解除によって回収漏れが生じないことを保証する」を追加（実現手段は DES-003 1.8 の DeleteOrphanRecords）。既存要件への影響表の DES-001 §8 行を「判定ポリシー・処理ロジック本体は変更しない（KEY ロック取得のみ追加）」に限定し、EXP-01/02 行との表現ぶれを解消                                                                                                                         |
| 2026-07-04 | k2moons | レビュー対応(stale 予約行の安全性): GC-02 の path 単位物理削除を「どの series からも参照されない record のみ削除（紐付きが残る record・復活済み record には不触。古い予約が残っていても実データを壊さない）」に強化。series 剥がしを伴う従来方式では予約解除漏れ時に復活済み文書が起動時に破壊され得たため（DES-003 1.9 参照）                                                                                                                                                                  |
